platform: linux

image_resource:
  type: docker-image
  source: {repository: busybox}

# inputs:
# - name: ci #added this because in real world you've to execute script from this dir

run:
  path: echo
  args: [executing unit test]
